# Task034 - Прогрессия 3️⃣

### Задание

Написать программу, которая вычисляет n-й член арифметической прогрессии.

Входные данные:
- `a1` — первый член прогрессии
- `d` — разность прогрессии
- `n` — номер члена, который нужно найти

Выходные данные:
- Значение n-го члена (double)

---

### Прототип функции

```c
double progression(int a1, int d, int n);
```

## Решение
Просто формула арифметической прогрессии: `An = A1 + (n - 1) * d`

Для запуска: 
```bash
gcc main.c -o main
./main
```

Для повторного запуска: `./main`